# Tests (local only)

The full unit test suite is maintained on development servers and is **not** shipped in this public install repository.

Only `test_ci.py` is included so GitHub Actions can lint the `tests/` path and run a minimal pytest check.
